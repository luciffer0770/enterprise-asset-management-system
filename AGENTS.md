## Cursor Cloud specific instructions

This repository is currently an empty starter/placeholder repo. It contains a single file (`asset1`) with no application code, dependencies, build configuration, or services.

- **No languages/frameworks**: No `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, or equivalent.
- **No services to run**: There are no backend, frontend, or database services.
- **No lint/test/build commands**: Nothing is configured.
- **No setup scripts**: No `Makefile`, `Dockerfile`, `docker-compose.yml`, or shell scripts.

When application code is added to this repository, update this file with relevant development instructions (how to install dependencies, run services, lint, test, and build).
